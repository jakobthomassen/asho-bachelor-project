name: Scheduled Ping

on:
  workflow_dispatch:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        env:
          URL: ${{ secrets.URL }}
        run: |
          curl -fsS "$URL" > /dev/null
